/** Initializes a new Hungry Square game and starts running it. */
class Main {
	function void main() {
		var HungrySquareGame game;
		let game = HungrySquareGame.new();
		do game.run();
		do game.dispose();
		return;
	}
}
